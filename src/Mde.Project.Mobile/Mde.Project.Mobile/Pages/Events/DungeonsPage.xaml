<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:Mde.Project.Mobile.ViewModels"
             xmlns:models="clr-namespace:Mde.Project.Mobile.Domain.Models"
             x:Class="Mde.Project.Mobile.Pages.DungeonsPage"
             x:DataType="viewmodels:DungeonViewModel"
             Title="{Binding Title}">

    <ContentPage.Content>
        <ListView 
            SelectedItem="{Binding SelectedDungeon, Mode=TwoWay}"
            HasUnevenRows="False"
            RowHeight="45"
            RefreshCommand="{Binding RefreshCommand}"
            RefreshControlColor="Green"
            IsPullToRefreshEnabled="True"
            SeparatorColor="Green"
            IsRefreshing="{Binding IsBusy, Mode=OneWay}"
            ItemsSource="{Binding Dungeons}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:DungeonModel">
                    <ViewCell>
                        <StackLayout
                            Padding="10, 10"
                            Orientation="Horizontal">
                            <Label Text="{Binding Name}" 
                                TextColor="Black"
                                HorizontalOptions="StartAndExpand"/>
                            <Label Text="{Binding Date, StringFormat='{0:dd/MM/yyyy @ hh:mm}'}"
                                TextColor="Black"
                                HorizontalOptions="End"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>